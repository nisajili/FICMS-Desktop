{
  "name": "@ficms/domain",
  "version": "0.1.0",
  "private": true,
  "description": "FICMS domain rules: roles, permissions, and pure business logic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@ficms/types": "workspace:*"
  },
  "devDependencies": {
    "@ficms/tsconfig": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
