# SQLite (WASM) file location for local development / standalone.
# The embedded standalone backend resolves its own data directory at runtime;
# this value is only a dev convenience.
DATABASE_URL="file:../../.ficms-data/dev.db"

# PostgreSQL (connected / on-premise deployments):
# DATABASE_URL="postgresql://ficms:ficms@127.0.0.1:5432/ficms"
